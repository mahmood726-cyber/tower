# Prefect Configuration for Tower
# Copy to .env and set your values

# Prefect API URL (leave empty for local ephemeral mode)
PREFECT_API_URL=

# Enable Prefect flows (0=off, 1=on)
TOWER_PREFECT_ENABLE=0
